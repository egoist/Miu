#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../markdown-pdf/bin/markdown-pdf" "$@"
  ret=$?
else 
  node  "$basedir/../markdown-pdf/bin/markdown-pdf" "$@"
  ret=$?
fi
exit $ret
